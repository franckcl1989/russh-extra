# Agent Skills

This directory contains the canonical skill files shared across all AI agent
runtimes. Skills are organized by topic, each in its own subdirectory with a
`SKILL.md` file.

Runtimes that support tool-specific skill variants may layer their own
configuration on top of these shared skills.
