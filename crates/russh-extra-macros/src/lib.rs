//! Proc-macro entry points for `russh-extra`.
//!
//! This crate is intentionally empty while the first public macro designs are
//! written. Macro behavior should be specified in `docs/dev/design/` before
//! being exported from this crate.

extern crate proc_macro;
