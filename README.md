# russh-extra
Ergonomic high-level async SSH abstractions for Rust, built on top of russh. russh-extra provides modern APIs for clients, servers, shells, PTY, SFTP, port forwarding, channels, and SSH protocol orchestration without exposing low-level packet and channel handling complexity.
