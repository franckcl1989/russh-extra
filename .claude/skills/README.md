# Claude Code Skills

Skills for this repository live in `.agents/skills/`. That directory is the
canonical location shared across AI agent runtimes.

If Claude Code needs tool-specific skill variants, add them here as separate
files. Do not duplicate the shared skills.
